import React from "react";

export const Header = ({title}) => {
    return (
        <div className="header-bg font-white font-size-30 font-weight-600 p-2">
            {title}
        </div>
    )
};
